apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-https
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: suadfehric@outlook.com
    privateKeySecretRef:
      name: letsencrypt-https-account-key
    solvers:
      - http01:
           ingress:
             class: traefik
